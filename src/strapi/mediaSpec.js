const media = require("./media");
const fs = require("fs");
describe("media", () => {
    describe("upload", () => {
        it("should return correct data", async () => {

            const output = await media.upload();
        });
    });
});