const {getCollections, getAllData, restoreData, restoreProdData} = require("./mongodb");

const revealed = {
    getCollections,
    getAllData,
    restoreData,
    restoreProdData
};

module.exports = revealed;
